#!/bin/sh

echo "🚀 プッシュ前のデグレチェックを実行中..."
echo ""

# テストを実行
npm run test

# テスト結果を確認
if [ $? -ne 0 ]; then
  echo ""
  echo "❌ テストが失敗しました。プッシュを中止します。"
  echo "   エラーを修正してから再度プッシュしてください。"
  exit 1
fi

echo ""
echo "✅ 全テストがパスしました。プッシュを続行します。"

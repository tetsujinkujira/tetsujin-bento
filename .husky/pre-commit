#!/bin/sh

echo "🔍 コミット前のデグレチェックを実行中..."
echo ""

# テストを実行
npm run test

# テスト結果を確認
if [ $? -ne 0 ]; then
  echo ""
  echo "❌ テストが失敗しました。コミットを中止します。"
  echo "   エラーを修正してから再度コミットしてください。"
  exit 1
fi

echo ""
echo "✅ 全テストがパスしました。コミットを続行します。"
